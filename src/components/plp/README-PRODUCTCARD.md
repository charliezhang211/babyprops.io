# ProductCard 组件

> 产品卡片组件 - 用于 PLP (产品列表页) 的产品展示

## 📁 位置

```
src/components/plp/ProductCard.astro
```

## 🎯 功能特性

1. **产品图片**
   - 1:1 方形比例
   - 悬停时图片放大效果 (scale-110)
   - 懒加载支持
   - 背景色使用 brand-light/20

2. **徽章系统**
   - 左上角: New / Featured 徽章
   - 右上角: Out of Stock 状态
   - 优先级: Sale > New > Featured

3. **产品信息**
   - 标题 (2行截断)
   - 价格 (使用 Playfair Display 字体)
   - 材质/色系小标签 (tag 样式)
   - 手工制作标签 (心形图标)
   - 道具尺寸信息

4. **快速加购按钮** (可选)
   - 悬停时从底部渐入
   - 带半透明黑色渐变背景
   - 仅在库存充足时显示

5. **交互效果**
   - 卡片悬停: 阴影加深 + 边框高亮
   - 图片悬停: 放大动画
   - 标题悬停: 颜色变为 brand

## 📋 Props 参数

```typescript
interface Props {
  product: CollectionEntry<'products'>;  // 必填: 产品数据
  showQuickAdd?: boolean;                // 可选: 显示快速加购按钮 (默认 false)
  class?: string;                        // 可选: 自定义 CSS 类
  loading?: 'lazy' | 'eager';            // 可选: 图片加载策略 (默认 lazy)
}
```

## 🎨 使用示例

### 基础用法

```astro
---
import { getCollection } from 'astro:content';
import ProductCard from '@/components/plp/ProductCard.astro';

const products = await getCollection('products');
---

<div class="grid grid-cols-1 md:grid-cols-3 lg:grid-cols-4 gap-6">
  {products.map((product) => (
    <ProductCard product={product} />
  ))}
</div>
```

### 启用快速加购

```astro
<ProductCard
  product={product}
  showQuickAdd={true}
/>
```

### 首屏产品 (预加载图片)

```astro
{products.slice(0, 4).map((product, index) => (
  <ProductCard
    product={product}
    loading={index < 4 ? 'eager' : 'lazy'}
  />
))}
```

### 自定义样式

```astro
<ProductCard
  product={product}
  class="shadow-lg"
/>
```

## 🎨 设计规范

### 颜色使用

| 元素 | 颜色 | Class |
|------|------|-------|
| 卡片背景 | 白色 | `bg-white` |
| 卡片边框 (默认) | 浅灰 | `border-gray-100` |
| 卡片边框 (悬停) | Brand 30% | `hover:border-brand/30` |
| 标题 (默认) | 深灰 | `text-gray-900` |
| 标题 (悬停) | Brand | `hover:text-brand` |
| 价格 | Brand Dark | `text-brand-dark` |
| 标签背景 | Brand Light 50% | `bg-brand-light/50` |
| 标签文字 | Brand Dark | `text-brand-dark` |

### 尺寸规范

| 元素 | 尺寸 |
|------|------|
| 卡片圆角 | `rounded-lg` (8px) |
| 图片容器 | `aspect-square` (1:1) |
| 标题字号 | `text-base` (16px) |
| 价格字号 | `text-lg` (18px) |
| 标签字号 | `text-xs` (12px) |
| 内边距 | `p-4` (16px) |

### 动画效果

| 效果 | 持续时间 | 类型 |
|------|----------|------|
| 卡片阴影/边框 | 300ms | `transition-all` |
| 图片缩放 | 500ms | `transition-transform` |
| 快速加购显示 | 300ms | `transition-opacity` |
| 标题颜色 | 默认 | `transition-colors` |

## 📦 响应式适配

### 网格布局建议

```astro
<!-- 移动端 1列 / 平板 3列 / 桌面 4列 -->
<div class="grid grid-cols-1 md:grid-cols-3 lg:grid-cols-4 gap-6">
  <ProductCard ... />
</div>

<!-- PLP 侧边栏布局: 移动端 2列 / 桌面 3列 -->
<div class="grid grid-cols-2 lg:grid-cols-3 gap-4">
  <ProductCard ... />
</div>
```

### 标签显示逻辑

- **手工制作标签:**
  - 移动端: 只显示图标
  - 桌面端: 图标 + "Handmade" 文字

## 🔗 关联组件

- **Badge.astro** - 徽章组件 (New/Sale/Pro/Featured)
- **Button.astro** - 按钮组件 (快速加购)
- **site.ts** - `formatPrice()` 价格格式化函数

## 🧪 测试要点

### 视觉测试

1. ✅ 卡片在不同屏幕尺寸下的布局
2. ✅ 图片加载和悬停动画
3. ✅ 徽章位置 (不重叠)
4. ✅ 标题长度截断 (2行)
5. ✅ 标签换行处理

### 功能测试

1. ✅ 点击卡片/标题跳转到 PDP
2. ✅ 快速加购按钮悬停显示
3. ✅ Out of Stock 时隐藏快速加购
4. ✅ 手工制作图标显示
5. ✅ 价格格式化正确

### 数据边界测试

1. ✅ 无材质/色系时不显示标签
2. ✅ 无道具尺寸时不显示尺寸信息
3. ✅ 非手工时不显示手工标签
4. ✅ 无徽章时不显示徽章容器

## 🚀 性能优化

1. **图片懒加载**
   - 首屏产品使用 `loading="eager"`
   - 其他产品使用 `loading="lazy"`

2. **减少重绘**
   - 使用 `transform` 而非 `width/height` 做动画
   - 使用 `opacity` 做显隐过渡

3. **CSS 优化**
   - 使用 Tailwind 预定义类
   - 仅在必要时使用自定义 `<style>`

## 📝 开发注意事项

1. **data 属性保留**
   - `data-component="product-card"` - 组件标识
   - `data-product-id` - 产品 ID (用于追踪)
   - `data-quick-add` - 快速加购按钮标识

2. **无障碍访问**
   - 图片必须有 `alt` 属性
   - 手工标签有 `title` 提示

3. **SEO 友好**
   - 使用语义化 `<article>` 标签
   - 标题使用 `<h3>` 层级
   - 所有链接可点击

## 🔄 未来扩展

### 可选功能

- [ ] 收藏按钮 (Wishlist)
- [ ] 颜色变体快速切换
- [ ] 产品评分星级
- [ ] "比较" 复选框
- [ ] 库存数量倒计时

### 性能优化

- [ ] 使用 Astro Image 组件替代原生 img
- [ ] 响应式图片 (srcset)
- [ ] WebP 格式支持

---

**Created:** Task-3.2
**Dependencies:** Badge.astro, Button.astro, site.ts
**Used in:** ProductGrid, FeaturedProducts, RelatedProducts
