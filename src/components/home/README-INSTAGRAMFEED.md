# InstagramFeed 组件文档

> **组件路径:** `src/components/home/InstagramFeed.astro`
> **任务:** Task-2.5
> **优先级:** P2 (可选)

---

## 📋 组件概述

InstagramFeed 组件用于在首页展示 Instagram 社交媒体内容，引导用户关注品牌社交账号，并展示买家秀图片。

### 功能特性

- ✅ Instagram 品牌渐变按钮
- ✅ 6 张图片网格展示（响应式）
- ✅ 悬停动画效果
- ✅ "Follow @Dvotinst" CTA
- ✅ 莫兰迪色系背景
- ✅ 占位符设计（可替换为真实图片）

---

## 🎨 设计规范

### 布局结构

```
┌─────────────────────────────────────────────┐
│        Follow Our Journey (标题)             │
│        描述文字 + Tag us @dvotinst           │
│        [Follow @dvotinst] (Instagram 按钮)   │
├─────────────────────────────────────────────┤
│  ┌───┐ ┌───┐ ┌───┐ ┌───┐ ┌───┐ ┌───┐       │
│  │ 1 │ │ 2 │ │ 3 │ │ 4 │ │ 5 │ │ 6 │  (6列) │
│  └───┘ └───┘ └───┘ └───┘ └───┘ └───┘       │
├─────────────────────────────────────────────┤
│   Share your work with @dvotinst (底部文案)  │
└─────────────────────────────────────────────┘
```

### 响应式断点

| 屏幕尺寸 | 列数 | 说明 |
|---------|------|------|
| 桌面 (lg) | 6 列 | 一行展示所有图片 |
| 平板 (md) | 3 列 | 两行展示 |
| 移动端 | 2 列 | 三行展示 |

### 色彩方案

| 元素 | 颜色 | 说明 |
|------|------|------|
| 背景 | `bg-brand-light` | 温暖奶油色 #F5F0EB |
| 标题 | `text-brand-dark` | 柔和棕色 #9C8B7E |
| Instagram 按钮 | 渐变 | 紫-粉-橙 Instagram 经典渐变 |
| 占位符 | `brand/20` + `brand-accent/20` | 淡雅渐变背景 |

---

## 📝 使用方法

### 基础使用

```astro
---
import InstagramFeed from '@/components/home/InstagramFeed.astro';
---

<InstagramFeed />
```

### 集成位置

在首页 `src/pages/index.astro` 中，建议放置在 **TrustBadges 之后**：

```astro
<HeroSection />
<CategoryGrid />
<TrustBadges />
<InstagramFeed />  <!-- 这里 -->
```

---

## 🔧 配置说明

### 数据源

组件当前使用 **静态占位符数据**。要添加真实图片，有两种方式：

#### 方式 1: 静态图片（推荐用于固定展示）

1. 将图片放入 `public/images/gallery/` 目录
2. 命名为 `instagram-1.webp` 到 `instagram-6.webp`
3. 取消注释组件中的 `<img>` 标签

```astro
<!-- 取消注释这段代码 -->
<img
  src={post.image}
  alt={post.alt}
  class="w-full h-full object-cover"
  loading="lazy"
/>
```

#### 方式 2: Instagram API（推荐用于动态更新）

如果需要从 Instagram 实时拉取数据，可以：

1. 申请 Instagram Graph API 访问权限
2. 创建 `src/lib/instagram.ts` 工具函数
3. 修改组件为服务端数据获取

```ts
// src/lib/instagram.ts
export async function getInstagramPosts() {
  const response = await fetch(
    `https://graph.instagram.com/me/media?fields=id,caption,media_url,permalink&access_token=${import.meta.env.INSTAGRAM_ACCESS_TOKEN}`
  );
  return response.json();
}
```

### 链接配置

Instagram 链接从 `site-settings.ts` 读取：

```ts
// src/config/site-settings.ts
social: {
  instagram: 'https://instagram.com/dvotinst',
}
```

---

## 🎯 动画效果

### 卡片悬停

- **缩放:** 1.05x
- **阴影:** 加深
- **叠加层:** 深色背景 + 爱心图标
- **过渡时间:** 300ms

### Instagram 按钮

- **默认:** 紫-粉-橙渐变
- **悬停:** 反向渐变 + 阴影 + 放大 1.05x

---

## 📱 移动端优化

- 2 列网格布局
- 间距从 4 (1rem) 保持一致
- 按钮宽度自适应
- 文字大小响应式调整

---

## ♿ 无障碍特性

- ✅ `target="_blank"` 链接带 `rel="noopener noreferrer"`
- ✅ SVG 图标带 `aria-hidden="true"`
- ✅ 图片带 `alt` 描述（当启用真实图片时）
- ✅ 键盘可访问的链接

---

## 🔍 SEO 考量

- 外部链接使用 `noopener noreferrer`（不传递权重但安全）
- 图片使用 `loading="lazy"` 懒加载
- 占位符不影响 SEO（无图片 = 无索引）

---

## 🧪 测试清单

### 功能测试

- [ ] Instagram 按钮点击跳转正确
- [ ] 图片卡片点击跳转正确
- [ ] 悬停效果正常
- [ ] 移动端布局正确

### 样式测试

- [ ] 品牌色正确应用
- [ ] 响应式断点正常
- [ ] 动画流畅无卡顿
- [ ] Instagram 渐变按钮正确显示

### 浏览器兼容性

- [ ] Chrome/Edge
- [ ] Firefox
- [ ] Safari
- [ ] 移动端浏览器

---

## 📦 依赖项

### 外部依赖

无（纯 Astro + Tailwind CSS）

### 内部依赖

- `@/config/site-settings` - Instagram 链接配置

---

## 🚀 未来优化方向

### Phase 1: 真实图片

- 替换占位符为真实买家秀
- 优化图片尺寸（建议 600x600）
- 添加图片压缩

### Phase 2: API 集成

- 集成 Instagram Graph API
- 自动拉取最新 6 张图片
- 添加缓存机制

### Phase 3: 交互增强

- 添加 Lightbox 查看大图
- 显示点赞数/评论数
- 添加图片标题/描述

### Phase 4: 内容优化

- 支持视频内容
- 添加"Load More"按钮
- 过滤特定标签的内容

---

## 📄 代码示例

### 修改展示数量

```astro
// 修改为 9 张图片
const instagramPosts = [
  // ... 添加更多项
];

// 更新网格列数
<div class="grid grid-cols-3 md:grid-cols-3 lg:grid-cols-9 gap-4">
```

### 修改文案

```astro
<h2>Follow Our Journey</h2>
<!-- 改为 -->
<h2>客户作品展示</h2>

<p>Tag us @dvotinst to be featured!</p>
<!-- 改为 -->
<p>使用 @dvotinst 标签分享您的作品</p>
```

### 修改按钮样式

```astro
<!-- 改为简约风格 -->
<a
  href={instagramUrl}
  class="bg-brand text-white px-8 py-3 rounded-full"
>
  Follow Us
</a>
```

---

## 🐛 常见问题

### Q: 为什么显示占位符而不是真实图片？

**A:** 需要手动添加图片到 `public/images/gallery/` 并取消注释 `<img>` 标签。

### Q: 如何更改 Instagram 账号？

**A:** 修改 `src/config/site-settings.ts` 中的 `social.instagram` 链接。

### Q: 可以只显示 4 张图片吗？

**A:** 可以，删除数组中多余的项，并调整网格为 `lg:grid-cols-4`。

### Q: 如何添加视频支持？

**A:** 需要修改数据结构添加 `type` 字段，并在模板中判断渲染 `<video>` 或 `<img>`。

---

## 📚 相关文档

- [CLAUDE.md - 项目规范](../../../CLAUDE.md)
- [TASKS.md - 任务清单](../../../TASKS.md)
- [site-settings.ts](../../config/site-settings.ts)
- [Instagram Graph API 文档](https://developers.facebook.com/docs/instagram-api/)

---

**创建日期:** 2026-02-03
**最后更新:** 2026-02-03
**维护者:** ULX Studio
